============
Installation
============

At the command line::

    $ easy_install django-cookies-samesite

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-cookies-samesite
    $ pip install django-cookies-samesite
